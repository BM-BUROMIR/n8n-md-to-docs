# Туннель для внешнего доступа к md2doc-converter

## Быстрый старт

Для создания публичного туннеля к вашему локальному сервису:

```bash
./expose.sh
```

Скрипт автоматически:
1. ✅ Проверит Docker контейнер
2. ✅ Проверит health endpoint
3. ✅ Попробует создать туннель через localhost.run
4. ✅ Если не получится, попробует serveo.net
5. ✅ Покажет публичный URL для доступа

## Что вы получите

После запуска скрипта вы увидите:

```
✓ Туннель создан: https://732c7b20372bb6.lhr.life

# Тестовые команды для проверки:
curl https://732c7b20372bb6.lhr.life/health

# Конвертация с формулами (требуется OAuth токен из n8n):
curl -X POST https://732c7b20372bb6.lhr.life/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_N8N_OAUTH_TOKEN" \
  -d '{
    "output": "# Test\\n\\n$$V = \\sum_{t=1}^{n} \\frac{ЧДП_t}{(1+r)^t}$$",
    "fileName": "Formula Test"
  }'
```

## Интеграция с n8n

В вашем n8n workflow (https://n8n.buromir.com):

1. Используйте HTTP Request node
2. URL: Публичный URL из туннеля (например `https://732c7b20372bb6.lhr.life/`)
3. Method: POST
4. Authentication: Google OAuth2 (n8n автоматически добавит Authorization header)
5. Body (JSON):
```json
{
  "output": "{{ $json.markdown }}",
  "fileName": "Converted Document"
}
```

## Остановка туннеля

Два способа:

1. **В терминале со скриптом:** Нажмите `Ctrl+C`
2. **Отдельной командой:**
```bash
./stop-tunnel.sh
```

## Альтернативные скрипты

Если нужен контроль над конкретным сервисом:

### localhost.run (рекомендуется)
```bash
./start-tunnel-localtunnel.sh
```

### serveo.net (если localhost.run недоступен)
```bash
./start-tunnel.sh md2doc
```

## Важные замечания

⚠️ **Для тестирования:** Туннели localhost.run и serveo.net подходят для разработки и тестирования.

⚠️ **URL меняется:** При каждом перезапуске туннеля URL будет новый (если не используете платный аккаунт).

⚠️ **Лимиты:** Бесплатные туннели могут иметь ограничения по трафику и времени работы.

✅ **Для продакшена:** Разверните сервис на сервере с nginx и SSL:
- См. `DEPLOY.md` для инструкций по продакшн деплою
- Используйте docker-compose на сервере
- Настройте nginx для проксирования
- Добавьте Let's Encrypt SSL сертификат

## Логи и мониторинг

```bash
# Логи туннеля
tail -f logs/backend-tunnel.log

# Логи сервера
docker compose logs -f md2doc-converter

# Проверка статуса
ps aux | grep ssh
docker ps | grep md2doc
```

## Troubleshooting

### Туннель не создаётся
```bash
# Проверьте, что контейнер работает
docker ps | grep md2doc-converter

# Проверьте health endpoint локально
curl http://localhost:8080/health

# Проверьте лог туннеля
cat logs/backend-tunnel.log
```

### URL не отвечает
```bash
# Проверьте, что туннель ещё активен
ps aux | grep "ssh.*localhost.run"

# Перезапустите туннель
./stop-tunnel.sh
./expose.sh
```

### Ошибка конвертации
```bash
# Проверьте логи сервера
docker compose logs md2doc-converter

# Убедитесь, что используете правильный OAuth токен из n8n
```

## Структура проекта

```
.
├── expose.sh                        # 🚀 Универсальный скрипт для туннеля
├── start-tunnel.sh                  # serveo.net туннель
├── start-tunnel-localtunnel.sh     # localhost.run туннель
├── stop-tunnel.sh                   # Остановка туннеля
├── test-local.sh                    # Тест локального сервера
├── logs/
│   ├── backend-tunnel.log          # Логи SSH туннеля
│   └── tunnel.pid                  # PID процесса туннеля
├── docker-compose.yml              # Docker Compose конфигурация
├── Dockerfile.prod                 # Production Docker image
└── DEPLOY.md                       # Инструкции по деплою
```

## Полезные ссылки

- localhost.run документация: https://localhost.run/docs/
- serveo.net: https://serveo.net/
- n8n: https://n8n.buromir.com
- Google Docs API: https://developers.google.com/docs/api

## Альтернативы для продакшена

Если вам нужен постоянный публичный URL:

1. **ngrok** (платный, но стабильный): https://ngrok.com/
2. **Cloudflare Tunnel** (бесплатный): https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/
3. **Nginx на VPS** (полный контроль): См. DEPLOY.md
